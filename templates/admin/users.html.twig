{% extends 'base.html.twig' %}
{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('assets/css/estilo.css') }}"/>
{% endblock %}

{% block body %}
    {% if app.user %}
            {% if app.user.role == "ROLE_ADMIN" %}
              <div class="panel panel-primary">
                <div class="panel-heading">Panel Principal</div>
                <div class="panel-body"><a href="/adminpanel/users">Info users</a></div>
                <div class="panel-body"><a href="/adminpanel/posts">Info posts</a></div>
              </div>
         {% endif %}
         <br>
          </div>
          <table class="table table-condensed">
            <thead>
              <tr>
                <th>Nom</th>
                <th>Email</th>
                <th>Password</th>
                <th>Accion</th>
              </tr>
            </thead>
            <tbody>
            {% for user in users %}
                <tr>
                <td>{{user.username}}</td>
                <td>{{user.email}}</td>
                <td>{{user.password}}</td>
                <td><a href="/adminpanel/{{user.id}}/edituser">Editar</a> / <a href="/">Eliminar</a></td>
              </tr>
            {%endfor%}
            </tbody>
          </table>
    {% endif %} 
{% endblock %}
